module.exports = {
    entry: './src/index.js',
    output: {
        filename: 'bundle[contenthash].js',
        path: path.resolve(__dirname, './dist'),
        publicPath: 'dist/'
    },

    plugins: [
        new UglifyJsPlugin(),
        new MiniCssExtractPlugin({
            filename: 'styles[contenthash].css'
        })
    ]

    After doing this need to use the contenthash generated file names into html.