plugin does something to our files:

minifiy js:

module: {
        rules: [
            {
                test: /\.(png|jpg)$/,
                use: 'file-loader'
            },
            {
                test: /\.js$/,
                exclude: /node_modules/,
                use: {
                    loader: 'babel-loader',
                    options: {
                        presets: ['transform-class-properties']
                    }
                }
            }
        ]
    },
    plugins: [
        new UglifyJsPlugin() <-- this is included within 'production' mode so dont need it
    ]